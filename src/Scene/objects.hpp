#pragma once

/* TO DO :
Create an abstract base class and then create objects that can be added to the scene (sphere, plan, cube...)

Each object can have the possibility to emit light.

Add texture to each object
*/